interface IArgs {
    x: number;
    velocity: number;
    angle: number;
    grav: number;
}
export declare const calculateTrajectory: ({ x, velocity, angle, grav }: IArgs) => {
    x: number;
    y: number;
};
export {};
