import React, { useState } from "react";

import Carousel from "./Carousel";

export default {
  component: Carousel,
  title: "Components/Carousel",
  argTypes: {},
};

export const Primary = () => <Carousel />;
